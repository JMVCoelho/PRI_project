for the python script:
./python exercise_4.py

it needs bs4 module installed (used for the scrapper)

Launch an http server before opening index.html (otherwise we wouldn't be able to fetch the json...)

We tested with python's SimpleHTTPServer and Chrome Extention "WebServer for Chrome"

/CSS --------------------------------------------- contains the CSS for the html
/JS ---------------------------------------------- contains the JS for the html
/Dataset_as_txt ---------------------------------- contains the scrapped documents
/Dataset_preprocessed ---------------------------- contains the preprocessed scrapped documents