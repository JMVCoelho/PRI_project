set foreground background colors w_gc somethingwhitepixel blackpixel macros server may return values suitable depth drawablessure fifth plane datacould different pixel values image fifth plane x might valuewant see plane data want better performance use clipmasks use pixmap stipple stippled rectangle fillmany servers stupid handling complex clip lists turn bitmap clip region hundreds hundreds little clipping rectangles clips every drawing primitive every one little trianglesactually must also ask faq popular reason graphics show wait expose event drawing rectangle